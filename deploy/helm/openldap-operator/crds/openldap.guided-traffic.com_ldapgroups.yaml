---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: ldapgroups.openldap.guided-traffic.com
spec:
  group: openldap.guided-traffic.com
  names:
    kind: LDAPGroup
    listKind: LDAPGroupList
    plural: ldapgroups
    singular: ldapgroup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.groupName
      name: Group Name
      type: string
    - jsonPath: .spec.ldapServerRef.name
      name: LDAP Server
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.memberCount
      name: Members
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: LDAPGroup is the Schema for the ldapgroups API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: LDAPGroupSpec defines the desired state of LDAPGroup
            properties:
              additionalAttributes:
                additionalProperties:
                  items:
                    type: string
                  type: array
                description: AdditionalAttributes allows setting custom LDAP attributes
                type: object
              description:
                description: Description is the group description
                type: string
              groupID:
                description: GroupID is the numeric group ID (gidNumber)
                format: int32
                type: integer
              groupName:
                description: GroupName is the LDAP group name (cn)
                type: string
              groupType:
                default: groupOfNames
                description: GroupType specifies the type of group (e.g., posixGroup,
                  groupOfNames)
                enum:
                - posixGroup
                - groupOfNames
                - groupOfUniqueNames
                type: string
              ldapServerRef:
                description: LDAPServerRef is a reference to the LDAPServer this group
                  belongs to
                properties:
                  name:
                    description: Name of the LDAPServer resource
                    type: string
                  namespace:
                    description: Namespace of the LDAPServer resource (optional, defaults
                      to same namespace)
                    type: string
                required:
                - name
                type: object
              members:
                description: Members is a list of user DNs or usernames that should
                  belong to this group
                items:
                  type: string
                type: array
              organizationalUnit:
                default: groups
                description: |-
                  OrganizationalUnit specifies which OU the group should be placed in
                  If not specified, defaults to "groups"
                type: string
            required:
            - groupName
            - ldapServerRef
            type: object
          status:
            description: LDAPGroupStatus defines the observed state of LDAPGroup
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the group's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              dn:
                description: DN is the full distinguished name of the group in LDAP
                type: string
              lastModified:
                description: LastModified is the timestamp of the last modification
                format: date-time
                type: string
              memberCount:
                description: MemberCount is the number of members in the group
                format: int32
                type: integer
              members:
                description: Members contains the list of current group members
                items:
                  type: string
                type: array
              message:
                description: Message provides additional information about the current
                  phase
                type: string
              observedGeneration:
                description: ObservedGeneration represents the .metadata.generation
                  that the condition was set based upon
                format: int64
                type: integer
              phase:
                description: Phase represents the current lifecycle phase of the LDAP
                  group
                enum:
                - Pending
                - Ready
                - Error
                - Deleting
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
