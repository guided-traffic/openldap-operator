---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: ldapservers.openldap.guided-traffic.com
spec:
  group: openldap.guided-traffic.com
  names:
    kind: LDAPServer
    listKind: LDAPServerList
    plural: ldapservers
    singular: ldapserver
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.host
      name: Host
      type: string
    - jsonPath: .spec.port
      name: Port
      type: integer
    - jsonPath: .status.connectionStatus
      name: Status
      type: string
    - jsonPath: .status.lastChecked
      name: Last Checked
      type: date
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: LDAPServer is the Schema for the ldapservers API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: LDAPServerSpec defines the desired state of LDAPServer
            properties:
              baseDN:
                description: BaseDN is the base distinguished name for LDAP operations
                type: string
              bindDN:
                description: BindDN is the distinguished name used to bind to the
                  LDAP server
                type: string
              bindPasswordSecret:
                description: BindPasswordSecret contains the reference to the secret
                  containing the bind password
                properties:
                  key:
                    description: Key within the secret containing the value
                    type: string
                  name:
                    description: Name of the secret
                    type: string
                required:
                - key
                - name
                type: object
              connectionTimeout:
                default: 30
                description: 'ConnectionTimeout in seconds (default: 30)'
                format: int32
                type: integer
              healthCheckInterval:
                default: 5m
                description: 'HealthCheckInterval defines how often to check the connection
                  (default: 5m)'
                type: string
              host:
                description: Host is the hostname or IP address of the LDAP server
                type: string
              port:
                default: 389
                description: 'Port is the port number of the LDAP server (default:
                  389 for LDAP, 636 for LDAPS)'
                format: int32
                type: integer
              tls:
                description: TLS configuration for secure connections
                properties:
                  caCertSecret:
                    description: CACertSecret contains the reference to the CA certificate
                      secret
                    properties:
                      key:
                        description: Key within the secret containing the value
                        type: string
                      name:
                        description: Name of the secret
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  clientCertSecret:
                    description: ClientCertSecret contains the reference to the client
                      certificate secret
                    properties:
                      key:
                        description: Key within the secret containing the value
                        type: string
                      name:
                        description: Name of the secret
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  clientKeySecret:
                    description: ClientKeySecret contains the reference to the client
                      private key secret
                    properties:
                      key:
                        description: Key within the secret containing the value
                        type: string
                      name:
                        description: Name of the secret
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  enabled:
                    description: Enabled indicates whether to use TLS/SSL
                    type: boolean
                  insecureSkipVerify:
                    description: InsecureSkipVerify controls whether the client verifies
                      the server's certificate
                    type: boolean
                required:
                - enabled
                type: object
            required:
            - baseDN
            - bindDN
            - bindPasswordSecret
            - host
            type: object
          status:
            description: LDAPServerStatus defines the observed state of LDAPServer
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the LDAP server's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              connectionStatus:
                description: ConnectionStatus represents the current connection status
                  to the LDAP server
                enum:
                - Connected
                - Disconnected
                - Error
                - Unknown
                type: string
              lastChecked:
                description: LastChecked is the timestamp of the last connection check
                format: date-time
                type: string
              message:
                description: Message provides additional information about the connection
                  status
                type: string
              observedGeneration:
                description: ObservedGeneration represents the .metadata.generation
                  that the condition was set based upon
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
